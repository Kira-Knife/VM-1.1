app:
  name: 'alertservice'
  version: '1.0.0'

http:
  port: '8787'
  cert_file: 'cert/cert.pem'
  key_file: 'cert/key.pem'


logger:
  log_level: 'error'
  rollbar_env: 'alertservice'

postgres:
  pool_max: 2
  pg_url: "postgres://admin:admin@localhost:5432/vmalertservice?sslmode=disable"
